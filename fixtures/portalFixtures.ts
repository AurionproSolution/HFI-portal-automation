/**
 * Shared portal fixtures — injects utils + table utils + HFI page objects.
 * Import via: import { test, expect } from "@fixtures/portalFixtures";
 */
export { test, expect } from "./hfiPortalTest";
export type { HfiPortalFixtures } from "./hfiPortalTest";
